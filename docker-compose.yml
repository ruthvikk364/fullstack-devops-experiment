version: "3.8"

services:
  nest:
    build: ./backend-nest
    ports:
      - "5000:3000"

  fastapi:
    build: ./backend-fastapi
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - nest
      - fastapi